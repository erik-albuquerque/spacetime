/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'avatars.githubusercontent.com',
      'github.com',
      'localhost',
      '192.168.100.30',
    ],
  },
}

module.exports = nextConfig
